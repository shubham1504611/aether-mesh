global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'aether-mesh-ingress'
    metrics_path: '/api/v1/metrics/prometheus'
    static_configs:
      - targets: ['backend:8080']
